#useradd - this commands creates a  user
#useradd -m  this adds user to home directory after creating the user, so user can have where to store their files.
#useradd -m -s this is similar to the previous command, plus it specifies their login shell. 
#adduser creates a new user interactively and guides you through setting up the account.
#passwd stands for password, this command creates password for user.
#usermod -aG the G stands for group, this command appends user to a group.
#usermod -s changes the login shell of an existing user.
#usermod -l the command is use for renaming user account,it changes user login name.
#userdel this command deletes a user.
#userdel -r removes user and their home directory.
 
