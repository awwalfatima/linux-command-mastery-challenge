#nano drill.sh
##!bin/bash
#read -p echo "enter your name" NAME
#echo "salam alaikum, $NAME"
#if [ -f "config.conf" ]; then echo "config exists" else echo "config file does not exists" fi
#SERVERS=("google.com" "cloudflare.com" "github.com")
#for SERVER in "${SERVERS[@]}" do echo "pinging $SERVER " if ping -c 1 -W 2 "$SERVER" > /dev/null 2>&1; then echo "$SERVER is reachable." else echo "$SERVER is not reachable." fi done
#touch config.conf
#ls -l
#chmod +x drill28.sh
#./drill28.sh