# #!/bin/bash (shebang) this is usually the first line of a bash script, which is called shebang
#chmod +x script.sh this command gives a file execute permission.
#./script.sh this executes a script located in your current directory.i.e ./hello.sh
#VAR=value variables allow you to store information.syntax: VAR=value; NAME="xahra"
#$() command substitution allows you to run a command and store/use its output inside another command.
#read -p this command allows your script to receive input from the user.
#if/ elif/ else / fi these allow bash to make decisions.they are called conditional statements. IF something is true,do this. ELSE IF something is true do this. OTHERWISE, do that.fi marks the end of the if
#-gt/ -lt/ -eq these are numeric comparison operators used in bash conditions.(-gt is greater than, -lt less than, -eq equal to)
#for loop a for loop repeats commands for a set of values.
#while loop keeps running as long as a condition remains true.