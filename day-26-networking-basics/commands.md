#ip a short for ip address, its displays the network interfaces on your linux machine and their IP addresses.    
#ip route ip route shows the routing table, the routing table tells linux where to send network traffic.
#ping -c tests whether another machine is reachable over the network, the -c option specifies how many ping packets to send.
#curl this is used to transfer data to or from a server using protocols such as HTTP and HTTPS.
#curl -I retrieves only the HTTP headers instead of donwloading the full webpage.
#wget is primarily used to download files from the internet.
#nestat -tulnp displays network connections, listening ports, and network statistics, is commonly used to see which ports are listening and which processes are using them.
#ss -tulnp is the modern tool for inspecting network sockets and listening ports.
#hostname displays the name assigned to your computer/server
#hostnamectl displays or manages the system hostname and related system information.