#groupadd adds a new group. groupadd then the name of the group
#groupdel deletes a group, groupdel the name of the group.
#gpasswd -a adds user to a group, gpasswd name of the user then group name
#gpasswd -d removes user from a group, gpasswd -d name of the user followed by groupname
#getent group shows information about groups, getent group, will show you all the groups that you have, and when you specify, it prints out informstion about that specific group.
#getent passwd looks up information about user account. getent passwd username
#groups shows whcih group a user belongs to.
#id -Gn this command shows the group id names.
#newgrp switches your current shell session to a different group, temporarily changing your active group ID.
#cat /etc/group displays the contents of the /etc/group file. 
