#nano ~/.bashrc this command opens .bashrc user configuration file
#source ~/.bashrc when you add an alias, this command helps you reload the file without closing your terminal
#cat ~/.bash profile this command displays the content of users bash profile 
#sudo nano /etc/environment this command opens the system wide environment configuration file as root using nano
#sudo nano /etc/bash.bashrc this command opens system wide environment configuration file as root, it contains bash configuration that can apply to all users interactive bash shells on system such as debian and ubuntu.
#alias this is used to customized or give a command a shorter name. alias name="command"
#unalias this command removes an alias, unalias alias_name, i.e unalias ll
#type this tells you what type a command is, it is an alias, a function, a keyword or an executable.
#which shows path to an executable that would be found through PATH
#whereis searches for related file associated with a command.