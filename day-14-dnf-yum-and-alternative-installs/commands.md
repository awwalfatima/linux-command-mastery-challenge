#(centos/RHEL uses dnf package manager.dnf(dandified yum))
#dnf update updates all the packages in the system, syntax : sudo dnf update, this upgrade all install software, checks the available packages,shows what will be updated.
#dnf install installs softwares, to install this is the command sudo dnf name of package, it will check for dependencies, and download everything needed.  
#dnf remove to uninstall the software, you do sudo dnf remove package.optionally remove the dependencies that are no longer required.
#dnf search searches through available package name and description. sudo dnf search name of the package.   
#yum install 
#rpm -qa
#snap install
#add-apt-repository
#dpkg -i
#pip/ npm install
